function task() {
  let firstName = prompt('enter your first name');
  let secondName = prompt('enter your second name');
  alert(`${firstName[0].toUpperCase()}. ${secondName[0].toUpperCase()}.`);
}
task();
